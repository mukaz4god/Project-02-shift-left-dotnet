name: CI Pipeline

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:

  sast-scan:
    uses: ./.github/workflows/sast.yml

  secret-scan:
    uses: ./.github/workflows/secret-scan.yml

  codeQL-scan-analysis:
    uses: ./.github/workflows/codeql.yml
    permissions:
      actions: read
      security-events: write
      contents: read

  dependency-check:
    uses: ./.github/workflows/dependency-check.yml

  image-scan:
    uses: ./.github/workflows/image-scan.yml
    permissions:
      contents: read
      security-events: write

  sbom:
    uses: ./.github/workflows/sbom.yml

  # defectdojo-import:
  #   runs-on: ubuntu-latest
  #   needs:
  #     - sast-scan
  #     - dependency-check
  #     - image-scan
  #     - sbom
  #     - secret-scan
  #     - codeQL-scan-analysis
  #   if: always() # Ensure this job runs even if previous jobs fail

  #   steps:
  #     - name: Download Semgrep artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: semgrep-results
  #         path: semgrep-results
  #       continue-on-error: true

  #     - name: Download Dependency-Check artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: dependency-check-report
  #         path: dependency-check-report
  #       continue-on-error: true

  #     - name: Download Trivy artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: trivy-results
  #         path: trivy-results
  #       continue-on-error: true

  #     - name: Debug downloaded files
  #       run: find . -type f

  #     - name: Verify DefectDojo secrets exist
  #       run: |
  #         test -n "${{ secrets.DEFECTDOJO_URL }}" || (echo "DEFECTDOJO_URL is missing" && exit 1)
  #         test -n "${{ secrets.DEFECTDOJO_API_KEY }}" || (echo "DEFECTDOJO_API_KEY is missing" && exit 1)
  #         test -n "${{ secrets.DEFECTDOJO_ENGAGEMENT_ID }}" || (echo "DEFECTDOJO_ENGAGEMENT_ID is missing" && exit 1)
  #         echo "DefectDojo secrets are configured"

  #     - name: Import Semgrep into DefectDojo
  #       if: hashFiles('semgrep-results/semgrep-results.json') != ''
  #       run: |
  #         curl -i -X POST \
  #           "${{ secrets.DEFECTDOJO_URL }}/api/v2/reimport-scan/" \
  #           -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
  #           -F "engagement=${{ secrets.DEFECTDOJO_ENGAGEMENT_ID }}" \
  #           -F "scan_type=Semgrep JSON Report" \
  #           -F "file=@semgrep-results/semgrep-results.json" \
  #           -F "active=true" \
  #           -F "verified=true"

  #     - name: Debug Dependency Check Content
  #       run: |
  #         ls -la dependency-check-report
  #         head -50 dependency-check-report/dependency-check-report.json

  #     - name: Import Dependency-Check into DefectDojo
  #       if: hashFiles('dependency-check-report/dependency-check-report.json') != ''
  #       run: |
  #         curl -i -X POST \
  #           "${{ secrets.DEFECTDOJO_URL }}/api/v2/reimport-scan/" \
  #           -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
  #           -F "engagement=${{ secrets.DEFECTDOJO_ENGAGEMENT_ID }}" \
  #           -F "scan_type=Dependency Check Scan" \
  #           -F "file=@dependency-check-report/dependency-check-report.xml" \
  #           -F "active=true" \
  #           -F "verified=true"

  #     - name: Import Trivy into DefectDojo
  #       if: hashFiles('trivy-results/trivy-results.json') != ''
  #       run: |
  #         curl -i -X POST \
  #           "${{ secrets.DEFECTDOJO_URL }}/api/v2/reimport-scan/" \
  #           -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
  #           -F "engagement=${{ secrets.DEFECTDOJO_ENGAGEMENT_ID }}" \
  #           -F "scan_type=Trivy Scan" \
  #           -F "file=@trivy-results/trivy-results.json" \
  #           -F "active=true" \
  #           -F "verified=true"

  aws-environment-promotion:
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/aws-environment-promotion.yml
    needs: defectdojo-import
